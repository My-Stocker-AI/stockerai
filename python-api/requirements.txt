fastapi==0.115.0
uvicorn[standard]==0.32.0
supabase==2.9.0
pydantic==2.10.0
python-dotenv==1.0.1
httpx==0.27.0
python-multipart==0.0.12
pdfplumber==0.11.9
# Verifies the Supabase login on every request. [crypto] pulls in the ES256 support the
# project's tokens are actually signed with — without it every login would be rejected.
PyJWT[crypto]>=2.9.0,<3.0.0
